# Задачи

## Сделано и развёрнуто (2026-09-03)

Всё из прошлого захода — панель юзера, панель админа, саммари с
разделителями, хаб группы (3 кнопки, авто-subscribe, `/online` без
`/compare`/`/top`) — в проде.

Плюс из сегодняшнего: `chat_seen` и переезд `/online` на список реальных
участников чата (не только подписчиков на публикацию), `/who` — кнопки
выбора игрока для `/stats` (отдельно от `/online`), приветственный текст с
объяснением, что бот вообще делает, и разбор «что нажать» на три случая.
Плюс настоящая причина заниженного «Всего»/«За месяц»: `title_history()`
просил только 200 игр, теперь 2000 — `scripts/reconcile_achievements.py`
прогнан после фикса.

## Сделано и развёрнуто (продолжение, 2026-09-03)

**Порог редкости и время/часовой пояс итога дня — теперь per-chat.**
`chat_settings` получила три nullable-колонки (`rare_threshold_percent`,
`daily_summary_time`, `timezone`) — `NULL` значит «следует за глобальным
`app_settings`», резолвится при чтении, не копируется при создании чата.
Планировщик (`poller/daily.py`) проверяет каждый чат своим временем и
поясом, не одним общим сравнением на всех. Админ-панель: в карточке чата
кнопки «Порог редкости ▸» и «Время итога ▸», у обеих — «✕ Сбросить на
глобальное». Дизайн — SPEC.md 5.5/5.7/6.4, реализация в коммите
`567248a`. Задеплоено 2026-09-04.

## Сделано и развёрнуто (продолжение, 2026-09-04)

Большой заход по списку правок от пользователя:

- **Обязательные per-chat настройки** — порог редкости/время итога/часовой
  пояс убраны из глобальных `app_settings` совсем, у каждого чата свои
  значения с момента создания (10%, 20:00, UTC+3). Миграция 009.
- **Режим редкости чата убран** (`chat_settings.rarity_mode`) — решает
  только личный выбор пользователя, чат оставляет только число-порог.
- **Часовой пояс — везде смещением от UTC**, не именем зоны: убран
  `COMMON_ZONES`/zoneinfo из админки, добавлен ручной ввод (`+3`, `-5`,
  `+5:30`) в личной панели, онбординге и карточке чата
  (`bot/util.py: parse_utc_offset`).
- **`/hltb`-лимиты разделены на два**: `hltb_results_limit` (пул поиска и
  подсказок, было зашито 20 для поиска) и `hltb_page_size` (было зашито 5).
- **Секретные ачивки — под спойлером** в публикации (`<span
  class="tg-spoiler">` на имени/описании, `has_spoiler=True` на картинке) и
  в `/recent` (см. ниже — там теперь тоже настоящий спойлер, не плейсхолдер).
- **Панель: «💬 Мои чаты ▸»** — список чатов, где юзера видел бот (подписка
  или просто `chat_seen`), с отписаться/подписаться/удалить на каждый.
  «Удалить» чистит и подписку, и `chat_seen` — чат пропадает из списка, но
  не забанен: подписался или написал в чате снова — вернулся.
- `/online` header переименован в «Онлайн-статус игроков».
- **Все моноширинные `<pre>`-таблицы (`/stats`, `/recent`, `/summary`,
  итог дня) заменены на строки-предложения внутри `<blockquote
  expandable>`.** `<pre>` рендерится как блок кода — для рейтинга игроков
  или списка игр это чужеродно. Общая инфа (шапка, счётчики, итог) —
  снаружи цитаты, сам список — внутри; для `/summary` итог теперь идёт
  перед списком, а не после. Заодно нашли (проверено вживую): `<blockquote>`
  вложенные теги держит нормально, в отличие от `<pre>` — значит `/recent`
  тоже получил настоящий Telegram-спойлер на имени секретной ачивки вместо
  плейсхолдера «🔒 Секретная».
- **Лимиты `summary_top_limit`/`stats_games_limit` — 0 значит «без
  ограничения».** Список и так лежит в цитате, урезать больше нечего.
  Кнопка «Показать все игры» в `/stats` за ненадобностью убрана целиком
  (`stats:allgames:` больше не существует) — у `/summary` кнопки «Показать
  всех» остались, они не про урезание, а про то, что цитата в одном
  сообщении не годится для реально большого списка на весь чат.

Дизайн — SPEC.md 1.4/1.6/5.5/5.7/6.1.1/6.2/6.3/6.4/6.6/7.1/7.2/7.3.
Задеплоено 2026-09-04.

## Сделано и развёрнуто (продолжение, 2026-09-04, вечер)

- **`rarity_mode` переехал с человека на пару (человек, чат)** — был единый
  переключатель на все чаты сразу (см. пункт 3 ниже, «один общий rarity_mode
  на все платформы»); теперь у каждого чата, куда публикуется человек, свой
  выбор `all`/`rare`/`hidden` (кнопка «Ачивки: …» в карточке чата панели
  «Мои чаты», не на главном экране панели). Миграция 016, без бэкфила
  старого значения — технически невозможно чистым SQL (SQLite резолвит
  все колонки на этапе prepare, даже в недостижимой ветке); у одного
  живого юзера из семи было отличное от дефолта значение, поправлено
  вручную после деплоя.
- **Форма сообщения об ачивке переделана**: заголовок — «{ник} получает
  достижение/трофей {кружок платформы} {платформа}», дальше через точку
  название игры, ««название ачивки»», гeймерскор (только Xbox) / место под
  цвет трофея (PSN, ещё не реализовано) / ничего (Steam), редкость — как
  раньше. Место под PSN уже есть, реализации трофеев пока нет.
- **Steam-поллер: grace-период на потерю presence.** Steam иногда на
  несколько минут перестаёт отдавать `gameid`, хотя человек продолжает
  играть — нашли вживую, ачивка задержалась на ~16–19 минут дважды за
  вечер. Теперь поллер до 10 минут после последнего подтверждённого
  `gameid` продолжает опрашивать ту же игру, если человек не оффлайн.
  `/online` не затронут — читает сырой, нерастянутый `gameid`.
- **`/delete_last`** — админ бота может в самом чате удалить последнее
  сообщение бота, не заходя в приватную админ-панель (там своя, массовая
  «стереть за 24 часа»).

Первые три — задеплоено 2026-09-04 вечером. `/delete_last` — закоммичено,
деплой не подтверждён.

## Сделано и развёрнуто (2026-09-05, днём)

- **Рефакторинг: общая математика интервалов пресенс-поллеров** вынесена в
  `poller/cadence.py` — `presence.py` (Xbox) и `steam_presence.py` дублировали
  три метода и две константы побайтово, кроме того, чьи поля датакласса
  читали. Свободные функции, не общий базовый класс — `tick()`/`_handle()`
  различаются слишком сильно (батч-запрос у Steam, `str` vs `int` состояние,
  grace-период только у Steam), чтобы наследование того стоило. Поведение
  не менялось, тесты зелёные до и после, плюс новые прямые тесты на
  `cadence.py`.
- **Дефолтный `rarity_mode` новой подписки — теперь в админке**, не зашит
  в `DEFAULT` колонки. `app_settings['default_rarity_mode']` (по умолчанию
  `'all'`, как и раньше), кнопка «Ачивки по умолчанию: … ▸» на главном
  экране `/admin`, тот же `next_rarity_mode`/`format_rarity`, что и у
  личного/чатового переключателей. На уже существующие подписки не влияет.

## Сделано, не развёрнуто (2026-09-05, вечер)

- **Форма сообщений стандартизирована заново** (закрывает пункт 14 ниже
  про дайджест, отменяет предыдущую версию заголовка с платформой из
  вечера 2026-09-04). Одиночное сообщение: гeймертег жирным + «получает
  достижение» (без ветки «трофей» — единая формулировка на все платформы),
  пустая строка, игра + платформа курсивом в скобках отдельной строкой,
  бейдж редкости перед названием достижения в кавычках, дальше гeймерскор
  и процент редкости через точку. **Гeймерскор показывается только если
  он не 0** — общее правило вместо платформенных списков-исключений (было
  отдельно для Steam, было место под цвет трофея PSN — оба убраны, ноль
  есть ноль на любой платформе).
- **Бейдж редкости — два состояния, не три**: 💎 «редкая» (≤15%), 🏆
  «обычная» (выше) — было 💎/⭐/ничего с двумя порогами. Без данных о
  редкости (Xbox 360) бейджа по-прежнему нет вообще — молчание, не «обычная».
- **Дайджест переделан под ту же форму**, один блок на игру (заголовок +
  список достижений), без суммы гeймерскора в шапке (та же причина, что и
  выше — для чисто Steam-сессии складывалось в лживое «+0 G»), с той же
  пустой строкой перед первым блоком. Рендерер группирует вход по
  `(platform, title_id)`, не по одному флоу целиком — сегодня `publish()`
  всегда отдаёт одну игру за раз, но рендерер теперь не завязан на это
  молча. Разные платформы вместе не группируются и не должны — обсудили
  специально, раздельные посты это нормально.
- **Перечисляются все достижения дайджеста, без «… и ещё N»** — лимит в 3
  строки на игру снят по просьбе.
- **Иконки дайджеста — галереей** (`sendMediaGroup`, до 10 штук, подпись на
  первой), а не одна фотография/голый текст. `PublishJob.photo_url`/
  `photo_has_spoiler` объединены в один `gallery: list[(url, secret)]` —
  одиночное сообщение теперь просто галерея из одного элемента, тот же
  путь доставки, что и у дайджеста, а не два похожих друг на друга.
- **Терминология: «достижение» везде, «ачивка» — нигде** (кроме сокращения
  «ач.», если где-то не хватит места) — слово встречалось в обеих формах
  вперемешку по всем сообщениям бота. Прошлись по всем строкам в `bot/`
  (хендлеры, `BotCommand`-описания, `plural_achievements`) — **кроме
  прозы в SPEC.md/TODO.md/README.md/CLAUDE.md/STRUCTURE.md**, там оба
  слова остаются как есть, это не то, что видят пользователи бота; можно
  дополнительно причесать по отдельной просьбе.

Деплой не подтверждён.

## В очереди — не начато

1. **AI-саммари чата** — по запросу выбор окна (24ч/12ч/6ч/3ч/2ч/1ч или
   100/200/300/400/500 сообщений), прогнать переписку за это время через
   Haiku 4.5: какие темы поднимались, кто какой позиции придерживался,
   плюс новостная сводка (юзеры иногда кидают новости). Сам вызов
   модели — простая часть; несколько вещей понадобится решить до кода:
   - бот сейчас **не хранит текст сообщений юзеров вообще** — нужна новая
     таблица (входящие, а не исходящие — `bot_messages` этого не покрывает)
     плюс политика хранения (сколько держать, когда чистить);
     это осознанная смена того, что бот вообще делает, не мелкая правка;
   - первая фича с реальной переменной стоимостью (токены Anthropic) —
     нужен кулдаун на чат минимум, возможно и лимит;
   - новый секрет в `.env` (API-ключ), новый клиент (httpx, без тяжёлого SDK,
     тем же стилем, что клиенты Xbox/HLTB);
   - промпт потребует итераций на реальной переписке — как отличать тему от
     шума, не путать, кто что сказал, что делать с фото/стикерами/форвардами;
   - стоит явный тумблер на чат («включить AI-саммари») — это доступ к
     переписке качественно другого уровня, чем всё остальное в проекте,
     не должно быть включено по умолчанию без ведома чата.

   Пользователь подтвердил: делаем, но не сейчас — «реализуем, но позже».

2. **Ачивки PlayStation.** Сложно в первую очередь из-за входа: у PSN нет
   официального API — обходной путь (`psnawp` и подобные) требует `NPSSO`,
   который человек вручную достаёт из куки на сайте PlayStation, и это
   не то, что можно попросить сделать 20-30 друзей массово. Плюс трофеи —
   не ачивки: нет прямого аналога гeймерскора (есть «уровень» по взвешенной
   сумме типов трофей, это другое число), процент редкости у трофея есть
   и ложится на существующую логику похоже. Данные — либо отдельный бот,
   либо обобщение схемы `seen_achievements` под несколько платформ.

3. **Ачивки Steam — реализовано (M-Steam-1 … 2e) и в проде.** Привязка
   аккаунта, клиент и кэш ачивок (`services/steam/`), presence-поллер и
   бэкфил при привязке (`poller/steam_presence.py`, `poller/steam_fetcher.py`),
   общая суммирующая логика в `/stats`/`/summary`/`/online`/панели — 2c/2d/2e
   ниже (SPEC.md, раздел 9). Живые ачивки идут в чаты каждый день. Остальной
   текст ниже — история решений по пути, не текущий план; известные
   оставшиеся дыры — отдельными пунктами ниже (`/recent` и таблица игр
   в `/stats` не видят Steam).

   Заметно проще PSN — у Steam есть официальный
   Web API, вход не нужен вообще: один API-ключ разработчика плюс SteamID
   человека (просто ссылка на профиль), никакого OAuth и токенов освежать
   не надо, только пользователь должен держать статистику игр публичной в
   настройках приватности Steam (сам тумблер не обойти). Значит, для Steam
   не нужны ни `services/crypto.py`, ни таблица `tokens`, ни
   `services/connect.py`, ни `web/oauth.py` — из того, что делает Xbox-часть
   сложной, тут почти ничего нет. Своя переделка: у ачивок Steam нет очков
   вообще (не гeймерскор, не уровень — просто факт разблокировки), редкость
   идёт отдельным вызовом на игру (`GetGlobalAchievementPercentagesForApp`),
   схема ложится ближе к `seen_achievements`, чем PSN (`xuid→steamid`,
   `title_id→appid`, `achievement_id→api name`).

   **Решено: команды общие, не по одной на платформу** — `/stats`,
   `/online`, `/recent`, `/summary` остаются как есть, без `_steam`/`_psn`
   вариантов. Разводится не набор команд, а то, что́ каждая показывает —
   и по-разному для каждой:

   - **`/stats [@user]`** — **пересмотрено** (была версия с отдельной
     таблицей на платформу и без общей суммы, отменена): счётчик ачивок
     («Сегодня»/«За месяц») — **один, суммой по всем платформам**: 3 на
     Xbox + 5 в Steam пишутся как «8 ачивок». Отдельными строками под
     гeймертегом — ник на каждой подключённой платформе («Steam: <ник>»,
     «PlayStation: <ник>»), просто для узнаваемости, без своих счётчиков
     рядом. Гeймерскор — **только Xbox**, для справки, не участвует ни в
     какой сумме (у Steam очков нет вообще, суммировать нечего). Игровая
     таблица (список игр за 30 дней) пока не пересматривалась отдельно —
     решить при реализации M-Steam-2/PSN, когда появятся реальные данные.
   - **`/online`** — одна строка на человека, не на платформу: берём самый
     свежий по `updated_at` статус среди его аккаунтов, а не фиксированный
     приоритет платформы. **Пересмотрено ещё раз**: кружок у имени всё же
     красит платформу (⚫ Steam, 🟢 Xbox — та же палитра, что и в `/stats`),
     не статус — статус только текстом рядом (`_presence_text`). Первая
     версия была специально без иконки платформы, чтобы нормализация
     Steam-presence в Xbox-термины (`chat_member_presence`, SQL) не текла
     наружу — при реализации оказалось, что для самой иконки платформу
     всё равно можно отдать отдельным полем `ChatPresenceRow.platform`,
     не трогая сами `state`/`title_id`/`title_name`. Смысл команды — беглый
     взгляд, две строки на одного и того же человека это ломают; полную
     картину даёт `/who` → `/stats`.
   - **`/recent`** — без изменений, проблем нет: общая хронологическая
     лента, не разбивается по платформам — единственное место, где
     смешение естественно (лента «кто что выбил, по времени», не рейтинг).
     Маленькая иконка платформы в строке; `+G` там, где очков нет, — «—»,
     как уже сделано в карточке `/hltb` для недостающих данных.
   - **`/summary`** — **пересмотрено так же, как `/stats`**: одно число
     ачивок на человека в лидерборде, суммой по всем платформам (была
     версия с платформой как вложенным разрезом внутри окна, отменена).
     Сортировка лидерборда — по этому суммарному числу ачивок, гeймерскор
     (Xbox-only) остаётся при числе только для справки, не как ключ
     сортировки — так же, как сейчас, для Xbox-only чатов ничего не
     меняется.

   Общая для всех четырёх модель данных: `users` перестаёт быть «один
   tg_id → один xuid», нужна отдельная таблица привязок на
   (tg_id, платформа) — один человек может держать несколько аккаунтов
   разом. Архитектурный вопрос «отдельный бот vs. общая схема с колонкой
   платформы» остаётся открытым, но UI-решение выше от него не зависит.

   **Панель: пересмотрено ещё раз при реализации, сделано иначе.** Идея
   «по кнопке на каждую платформу, каждая — свой переключатель
   любые/только редкие/скрыто» отклонена: та же логика, что раньше убрала
   чатовый rarity-тумблер как избыточный второй переключатель (раздел 1
   SPEC.md) — по кнопке на каждую новую платформу это просто накопление
   одинаковых кнопок, а не архитектура, и ничего не объясняло, зачем
   человеку разный режим на Xbox и в Steam. **Сделано: один общий
   `rarity_mode` на все платформы сразу** (кнопка «Ачивки: …» в панели,
   `SPEC.md` разделы 1.4/5.5/9 M-Steam-2e) — Xbox 360, у которого нет
   данных о редкости вообще, просто исключён из фильтра «только редкие»
   (показывается как в «любые»), вместо отдельного тумблера рядом. Старая
   отдельная кнопка «Xbox 360: показывать/не показывать» удалена.

   **Иконка платформы — цветной кружок**, сделано в шапке `/stats` (🟢
   Xbox, ⚫ Steam, 🔵 PlayStation, когда появится PSN) — не в панели, там
   после отказа от кнопки на платформу иконке ставить уже некуда.

4. **Описание игры в `/hltb`** — отложено, источник текста не найден.
   Формат карточки понравился (сворачиваемая цитата, `<blockquote expandable>`,
   с иконкой 📖) — вопрос только в тексте внутри. Проверено вживую:
   - **Steam Store API** (`storesearch` + `appdetails`, без ключа) — не
     годится содержательно. `short_description` — одна маркетинговая фраза
     на игру, `about_the_game` — HTML-мешанина (видео, буллет-список фич,
     иногда декоративные глиф-иконки внутри текста вместо букв), нужен
     реальный HTML-парсер и всё равно получается список фич, не связное
     описание. Технически часть (эндпоинты публичные, `cc=US` + `l=russian`
     даёт готовый русский текст без переводчика — но искать и запрашивать
     обязательно с `cc=US`, не `cc=RU`: часть игр, заблокированных в
     российском Steam-магазине, например Helldivers 2, с `cc=RU` пропадают
     из выдачи или отдают `success: false`) была решена, содержательная —
     нет.
   - Рассмотренные, но не проверенные вживую альтернативы: **IGDB**
     (официальная-ish база игр через Twitch API — чистое поле `summary`, не
     маркетинг, но нужна регистрация приложения в Twitch и новый секрет);
     **LLM сам пишет** (Haiku, кешируется навсегда — но первая фича с
     реальной стоимостью в токенах, тот же вопрос уже стоит у AI-саммари);
     **Википедия** (бесплатно, без ключа, но у нишевых/старых/инди игр
     часто нет статьи в рувики, и стиль энциклопедический, не игровой обзор).

   Решение отложено, к варианту не возвращались.

5. **Статус серверов Xbox Live.** Показать, есть ли сейчас проблемы у
   сервисов Xbox Live (что-то вроде статус-страницы Microsoft). Пользователь
   подтвердил: в очередь, не сейчас.

6. **Продумать флоу логина Steam.** Повод: whalerider84 (tg_id 127383366)
   не смог подключить Steam, причина не установлена — `/connect_steam`
   ничего не логирует ни на одном из отказов (пустой аргумент, профиль не
   найден, закрытая статистика), только сам факт успешной привязки. По
   логам не восстановить, что именно он присылал. Обсудить заново: что
   идёт не так у людей на этом шаге, и стоит ли хотя бы залогировать
   попытки на будущее, раз это уже второй раз всплывает вслепую.

7. **`/who`: сообщение «Чья статистика интересует» не исчезает** после
   выбора игрока — должно закрываться/редактироваться, как это принято в
   остальных местах бота (сравнить с тем, что происходит после выбора в
   похожих клавиатурах).

8. **`/who`: нет кнопки «Отмена»** в том же сообщении «Чья статистика
   интересует» — некуда деться, кроме как выбрать кого-то.

9. **Автоудаление служебных сообщений через 5 минут.** Служебные — всё,
   КРОМЕ результатов `/stats`, `/summary`, `/online`, `/hltb` и `/recent`
   (то есть подсказки, промпты выбора, подтверждения и т.п.). Нужно на
   реализации явно определить границу — что именно считать «служебным» в
   каждом хендлере, единого списка сообщений сейчас нет нигде в коде.

10. **Админ: отдельно удалять только служебные сообщения.** Дополняет
    `/delete_last` (последнее сообщение бота, только что сделано) и
    существующее полное «стереть за 24 часа» в приватной админ-панели —
    третий, промежуточный вариант: почистить накопившийся служебный мусор,
    не трогая опубликованные ачивки/статы/саммари. Зависит от пункта 9 —
    нечего фильтровать «только служебные», пока не решено, что ими
    считается.

11. **В `/stats` нет ачивок Steam в таблице игр.** Известная, осознанная
    дыра (см. пункт 3 выше и `handlers/chat.py`'s `_build_stats_text`) —
    таблица «Игры за 30 дней» построена на `recent_games(xuid, …)`,
    Xbox-only с самого начала M-Steam-2c, решение отложено до появления
    реальных Steam-данных. Данные есть уже давно — пора решить.

12. **В `/stats` разделить статистику 24ч/30 дней по платформам.**
    ⚠️ Разворот уже принятого решения: пункт 3 выше прямо фиксирует
    «пересмотрено — один общий счётчик суммой по всем платформам»
    (и то же самое для `/summary`'s лидерборда) — версия с разбивкой по
    платформе обсуждалась и была отменена. Стоит явно решить заново, не
    молча переписать поверх старой аргументации — уточнить, что изменилось
    во мнении.

13. **В `/recent` нет ачивок Steam — похоже на настоящий баг, не дыру
    в дизайне.** Проверено: `chat_recent()` (`db/repo.py`) делает
    `JOIN seen_achievements s ON s.xuid = u.xuid` — INNER JOIN по `xuid`,
    так что человека без Xbox-аккаунта вообще не видно, а с Xbox-аккаунтом
    видно только Xbox-строки. При этом TODO и SPEC (M-Steam-2e) прямо
    говорят, что `/recent` — «без изменений, проблем нет», общая лента
    должна была просто подхватить Steam через `platform`-иконку на
    строке — JOIN под это не переписали. Похоже, реализацию признали
    готовой раньше, чем она правда стала такой.

14. ~~Поправить форму дайджеста~~ — сделано 2026-09-05 вечером вместе с
    полной переделкой формы сообщений (см. «Сделано, не развёрнуто» выше).
