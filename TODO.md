# Задачи на 2026-09-04

Список с конца сессии 2026-09-03.

1. **Поднять VPS с доменом.** Сейчас бот работает на домашнем ПК через
   Cloudflare quick tunnel — адрес временный, слетает при перезапуске
   туннеля. Домен на Spaceship уже есть, можно взять поддомен.

2. **Поправить отображение ачивок за месяц (синхронизация).**
   Проверить — возможно, частично уже закрыто 2026-09-03: месячное окно
   сводки стало скользящим (30 дней вместо календарного месяца), и починен
   баг, из-за которого юзеры без единой ачивки за период пропадали из
   таблицы целиком (`INNER JOIN` → `LEFT JOIN` в `chat_member_stats`).
   Свериться с тем, что имелось в виду, прежде чем переделывать.

3. **Нормальное сравнение юзеров по одной конкретной игре.**
   Заготовка есть в SPEC.md как M7 («идея, не спроектирована») — данные уже
   кэшируются в `title_history`, нерешён только вопрос выбора игры (список
   последних / поиск / инлайн-режим).

4. **Проработать саммари (дневное, месячное).**
   Уже переделывалось 2026-09-03: окна 24 часа / 30 дней, HTML-таблица
   (`bot/services/tables.py`), зелёные кружки добавляли и убрали по фидбэку.

5. **Продумать кнопки бота.**
   Сейчас клавиатуры (хаб группы, панель юзера) собирались по одной кнопке
   за раз, без единого продуманного набора.

6. **Проработать админ-панель.**

7. **Панель юзера в личке** — отдельно перепроверить, как работает
   мультичатовость (один юзер может быть подписан сразу в нескольких чатах;
   логика на месте, но не перепроверялась после правок по таблицам/редкости).

8. **Больше информации об ачивках в панели юзера.**
